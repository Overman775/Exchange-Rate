name: exchange_rate
description: Exchange rate witch block and hive.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^5.0.0
  flutter_bloc: ^5.0.0
  hive:
  hive_flutter:
    git:
      url: git://github.com/hivedb/hive.git
      path: hive_flutter
  equatable: ^1.2.0
  http: ^0.12.1

dev_dependencies:
  hive_generator:
    git:
      url: git://github.com/hivedb/hive.git
      path: hive_generator
  lint: ^1.2.0
  
  flutter_test:
    sdk: flutter
  


flutter:
  uses-material-design: true
